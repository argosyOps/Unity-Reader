using UnityEngine;
using System.Collections;

public class renderBounds : MonoBehaviour {

	// Use this for initialization
	void Start () {
//		//Get the width of sprite box
//		Debug.Log (renderer.bounds.size.x);

//		//The following code can be used to resize the camera based on the width of the box
//		Camera.main.orthograpthicSize = widthToBeSeen * Screen.height / Screen.width * 0.5;
	}
	

}
